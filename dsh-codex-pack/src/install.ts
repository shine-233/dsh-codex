// v0 integration stub: real Cordis Service wiring lands after module npm publish.
export function install(ctx?: unknown): void {
  console.log('[dsh-codex-pack] v0 scaffold — see cordis.patch.yml.example & MOUNT_POINTS.md');
}
