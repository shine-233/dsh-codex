# codex→dsh 移植套件 · 路线图（2026-09-06，同日已执行两轮）

> 基线上游：`openai/codex@970b7f2ff4f6`（0.149 时代，2026-08-21）→ 本轮对齐目标 `rust-v0.153.4`（2026-09-04）。
> 台账：`dsh-codex-ledger/coverage.yaml|json`（150 单元，verify_coverage.py 对 0.153.4 树 `[OK]`）。
> 审计口径：`status: implemented(已实现) / distilled(蒸馏) / design-only(仅设计收割)`，逐条有 `audit` 证据行。
> **当前分布（2026-09-06 文档起点）：implemented 3 · distilled 18 · design-only 39 · E0 待准入 0。**
>
> **⚠️ 2026-09-07 对账复核（见 ROADMAP-2026-09-07-PHASE2.md）**：上表「design-only 39」是 2026-09-06 多轮执行**前**的快照。经逐条核对权威台账 `dsh-codex-ledger/coverage.yaml` 并**实跑 10 包 211 测试**（全绿），当前真实分布为 **implemented 7 · distilled 46 · design-only 0（已准入能力 100% 有可运行实现）**。原 39 条中：32 条已在 09-06 四/五轮落地为 distilled；4 条本就改判 EXCLUDED/E5（遗留 design-only 标记已于 09-07 清除）；3 条（bwrap/process-hardening/shell-escalation）为 vendor 二进制内嵌行为、无独立 JS 面，09-07 改判 EXCLUDED/E5-vendor-binary。另：实跑抓到 1 个真实缺陷（config-importer pathUri 在 win32 下 POSIX 路径被错误反斜杠化），已修复并回归 18/18 绿。

## 一、本轮（2026-09-06）已完成

1. **codex-schema 对齐 0.153.4**：app-server-protocol 的 ts-rs bindings 纯镜像同步（28 增/32 改，含 v2 新面 613 文件）；手写草图补 4 个新变体（`EnvironmentConfigState::FromThread`、`ReasoningEffort::Persistent`、`RolloutItem::TokenUsageRecord/RealtimeItem`）；插件锚点常量更新。
2. **shell-command 危险命令分类器移植落地**（销掉最大虚标）：`codex-policy-engine/src/commandSafety.ts` —— rm 强制删除 / sudo·env·trap 包装递归 / `sh -c` 字面量递归 / 深度上限；Windows 侧 PowerShell·CMD·GUI 直启的 URL 与强制删除模式；**含 0.153.4 的 Windows executable_basename 盘符修复**。插件缝新增 `codex_command_safety_check` 只读工具与 `commandSafety: off|audit|enforce` 审批闸（默认 off，不改变既有行为）。12 测试。
3. **utils/output-truncation 移植落地**：`src/outputTruncation.ts`（bytes/tokens 双策略、中间截断标记、空文本项跳过、省略计数）。8 测试。
4. **prompts 资产核实**：13 个模板资产与 0.153.4 逐字节一致，无需刷新（上游 +5.6k 变更在蒸馏面外）。
5. **sandbox-bin 重 vendor**：Windows 两 exe 更新至 0.153.4 官方 release 资产（上游已不再发布 linux-sandbox 资产）；冒烟测试通过。
6. **台账诚实化**：58 条 dest 逐条审计分级；`plugin/core-plugins/utils/plugins` 改判 `EXCLUDED/E5`（dsh cordis 插件体系即等价层）；上游 8 个新 crate 登记 `E0-pending-admission`；verify_coverage.py 对 0.153.4 树 `[OK] 150 manifests`。

**当前分布**：implemented 3 · distilled 18 · design-only 37（另有 8 条 E0 待准入）。

6. **shell-command/output-truncation 升级为 distilled**；prompts 资产核实与 0.153.4 一致（未刷新即最新）；Windows 沙箱二进制重 vendor 至 0.153.4。
7. **E0 全部准入**（8/8，按建议判 E1/E4/E5 或归 prompts/config-importer），E0 清零。
8. **补审两个遗留 diff**：exec-server-protocol protocol.rs +59 无类型级变更；code-mode-protocol runtime.rs 新增 `MissingCodeModeHostDuration`（蒸馏面外）。均无动作。
9. **pack 升级**：4 行 stub → `src/index.ts` 台账体检工具（loadLedger/summarizeByModule/statusReport/pendingAdmissions）+ `codex_pack_status` 只读工具；4 个新测试。
10. **session-kit 回归加固**：新增 0.153.4 新 RolloutItem 变体（token_usage_record/realtime_item）的容忍测试。
11. **codex-kit 开启 commandSafety enforce**（用户层 patch，已注明日期与原因），组合树验证通过；tmp-plugin-check 用户层保持原样。

**回归底线达成**：9 模块 61+ 测试全绿（policy-engine 28、pack 9、session-kit 6、edit-fusion 10、skills-kit 5、redteam 系 3-5、sandbox-bin 2、schema 1…），全部 profile dump 通过，真启动冒烟 2/2。

## 一⵾、第五轮（2026-09-06 同日）：schema 四协议面 M1 → 可执行校验全部完成

1. ✅ `handwritten/history/validate.ts`（第四轮）：RolloutItem 十变体守卫。
2. ✅ `handwritten/protocol/validate.ts`：UserInput 七变体 / ParsedCommand 四变体 / RawFileSystemPath 三变体 / EnvironmentConfigState（含 0.153.4 FromThread）/ ReasoningEffort 全变体集（含 Persistent 与 Custom 载荷）。
3. ✅ `handwritten/exec-server-protocol/validate.ts`：NetworkPolicyDecision（Deny/Ask 必带 reason）/ RequestId / JSONRPCMessage（Response 需 result|error、Error 需 code+message）。
4. ✅ `handwritten/code-mode-protocol/validate.ts`：WireContentItem / WireRuntimeResponse / RuntimeResponse / WaitOutcome / WaitToPendingOutcome / ExecuteToPendingOutcome / FunctionCallOutputContentItem。
5. 说明：**零依赖手写守卫**而非 zod——zod 在 schema 包是未声明的传递依赖，硬引入会造成幻影依赖；`types.ts` 草图保留 `@ts-nocheck`（形状仍是子集），校验器以 `{ok, variant, error}` 三元组暴露判定，15 个测试（三面 11 + history 4）。
6. ⏳ **linux musl 自建维持阻塞**：本机无 docker/Rust 工具链；待有 Linux 构建环境时执行。

## 一‴、第四轮（2026-09-06 同日）：差分测试设施（蒸馏忠实度的机器级验证）

1. ✅ **向量抽取器**：`differential/extract-command-safety.mjs` —— 从上游 Rust 测试文件机器提取 `vec_str` 测试向量（支持转义字符串/原始字符串 `r#"..."#`/分组断言/动态向量跳过计数），输出带溯源的 JSON。
2. ✅ **向量库**：`differential/vectors/commandSafety.json`（55 条：posix 16 + windows 39）+ `outputTruncation.json`（9 条，手工转录自 truncate_tests.rs 的 assert_eq 精确值）。
3. ✅ **差分运行器**：`test/differential.test.ts`（vitest 常驻）—— 64 向量全部按上游断言比对。
4. ✅ **首轮差分即抓到 4 个保真度缺口并全部修复**：looks_like_url 缺少"token 内 http(s):// 前缀定位"预处理（Start-Process('https://…') 单 token 场景）；formattedTruncateText 的 token 策略预算比较错用原始 token 数（应为 ×4 字节）；truncateWithByteEstimate 的 prefix 初始化错误（leftBudget=0 时前缀应为空）；分类器未接受 `-lc` 旗标。**修复后 64/64 全过。**
5. ✅ **结论修正**：蒸馏忠实度不再是"人工复核"背书——55 条命令安全向量 + 9 条截断向量与上游逐字节断言一致，这是机器验证。

**刷新方式**：检出任意上游 tag 到本地 → `node differential/extract-command-safety.mjs <codex-rs-dir> <vectors输出>` → 重跑 pack vitest。季度对齐时对 0.154+ 重复此流程即可持续量化蒸馏忠实度。

## 一″、第三轮（2026-09-06 同日）：收割设计与遗留项执行

1. ✅ **command_canonicalization 蒸馏落地**：`codex-policy-engine/src/canonicalization.ts` —— 审批规范键（`/bin/bash -lc X` 与 `bash -c X` 同键、复杂脚本进 `__codex_shell_script__` 前缀形、powershell 同理），并接入插件审批流 `evaluateCached`（unified_exec 重试缓存思想的蒸馏：同键裁决会话内稳定免重复计算；注：用户审批结果回馈缝 dsh 未暴露，缓存仅做裁决记忆化——代码注释已如实声明）。6 测试。policy-engine 总计 42。
2. ✅ **context_manager token 估算蒸馏**：`codex-session-kit/src/contextEstimation.ts` —— 4 bytes/token、7,373 字节/图（≈1,844 tokens）、内联 base64 折扣替换、用户轮次边界；4 测试。
3. ✅ **rollout 冷压缩蒸馏**：`codex-session-kit/src/sessionCompression.ts` —— 冷文件 gzip + 运行标记防重叠 + 透明读取（上游用 zstd，此处零原生依赖用 gzip，格式差异已声明）；4 测试。
4. ✅ **schema M1 → 可执行校验（第一批）**：`handwritten/history/validate.ts` —— RolloutItem 十变体运行时守卫（含 0.153.4 两个新变体），零依赖；4 测试。
5. ✅ **musl 阻塞确认**：本机无 docker/Rust 工具链，linux-sandbox 0.153.x 自建不可行——维持 0.149 vendor + 台账声明，待有 Linux 构建环境时执行。

## 一′、第二轮（2026-09-06 同日）：五个真缺口全部处置

1. ✅ **shell 语义解析器**：`codex-policy-engine/src/shellParser.ts` —— 引号/注释/控制算符/重定向/**命令替换 $( ) 与反引号**/heredoc；`dangerousCommandMatchLine` 接入分类器，堵住 `echo $(rm -rf /)` 式逃逸；6 测试。policy-engine 总计 36 测试。
2. ✅ **技能执行器**：`codex-skills-kit/src/executor.ts` —— skillRead（权限围栏/256KiB 上限/分页游标，对应上游 tools/read.rs）+ skillList；5 测试。
3. ✅ **rollout 回放**：`codex-session-kit/src/replay.ts` —— seq 编号回放、atSeq fork/resume 切片、消息抽取、坏行计数；4 测试。
4. ✅ **结构化记忆**：`codex-session-kit/src/structuredMemory.ts` —— 上游文件格式（raw_memories.md 分节 + rollout_summaries/）+ 增长守卫 rebuild + forget + 大小上限；5 测试。
5. ✅ **exec-server 查证后改判 E5**：它是 codex 自有架构的 JSON-RPC 子进程控制服务器（pty + 沙箱 helper 调度），dsh 的 subprocess/terminal/sandbox 层即等价层，44k 行移植属重复建设；协议类型已由 codex-schema 蒸馏。
6. ✅ **18 个蒸馏件忠实度复核**：逐个对照 0.153.4 源码与其声明的蒸馏面——未发现虚标；每个蒸馏件的头部注释/README 都如实声明了范围与简化点。
7. ✅ **core 29.8 万行收割点审计**：识别出 6 个可收割设计（见下）。

**core 可收割点（core/src 扫描结论，供后续蒸馏立项）**：
- `unified_exec`：交互式进程执行的审批+沙箱编排，**沙箱拒绝后免重问的重试缓存**（`is_likely_sandbox_denied` 启发式）——可直接借鉴进 policy-engine 审批流。
- `context_manager`：history 归一化 + **token 估算（含图片字节）+ 用户轮次边界判定**——可反哺 dsh compaction。
- `command_canonicalization.rs`：命令规范化形态用于规则匹配——可提升 policy-engine 前缀规则的命中率。
- `rollout/compression.rs`：会话日志压缩——session-kit 回放的存储面。
- `guardian/`：同步审查 + 异步评分的上下文组织——prompts/审批双缝设计。
- `codex_delegate.rs`：任务委托模式。

## 二、待决策（2026-09-06 已全部执行完毕，留档备查）

| 决策 | 执行结果（2026-09-06） |
|---|---|
| 8 条 E0 新 crate 准入 | 已按建议落账：config-schema→E4；guardian-context→codex-prompts（design-only）；otel-trace-websocket→E5；worktree/agent-roles→E5；realtime-webrtc→E1；voice-host→E4；utils/git-discovery→codex-config-importer（design-only） |
| codex-policy-engine 的 commandSafety 闸默认开启 | 已在 codex-kit profile 开启 enforce（windows 平台），默认值仍为 off；误报率待真实会话观察 |
| shell-command 的 parse_command 全量解析器（2,766 行）是否值得移植 | 维持缓议：当前启发式覆盖主流危险模式；仅当需要完整 shell 语义（命令替换、heredoc）时再立项 |

## 三、下轮对齐（2026-11 季度 diff 的建议范围）

按本轮同口径重跑（verify → 审计 → 对齐 → 回归）。2026-09-06 已提前完成的项标 ✅：

1. ⏳ **codex-session-kit**（上游 +5.0k）：ext/memories 结构化记忆、rollout-trace、thread-store 的蒸馏优先级评估；✅ 解析器对新 RolloutItem 变体的显式容忍测试已加。
2. ⏳ **codex-schema 手写面**：✅ exec-server-protocol protocol.rs（+59）与 code-mode-protocol runtime.rs（+94）已复核（无类型级触及，详情见台账 audit 行）；⏳ protocol 草图从 M1 升级为带 zod 校验的正式形状。
3. ✅ **dsh-codex-pack**：台账体检工具已落地（loadLedger/summarizeByModule/statusReport + `codex_pack_status` 工具 + 4 测试）；core-plugins/plugin/utils/plugins 已改判 E5。
4. ⏳ **codex-sandbox-bin linux 面**：搭 musl 构建环境（需 Rust 工具链）自建 0.153.x linux-sandbox；或改判 vendor 停更、声明 0.149（本机已完成 Windows 侧重 vendor）。
5. ✅ **回归底线**：全模块 vitest + 各 profile `--dump-config` + 真启动冒烟（本轮 2/2 通过）。

## 四、长期原则（沿用既有决策）

- E1（OpenAI 云绑定）/E2（终端 UI）/E3（V8 原生）/E4（Rust 胶水）排除理由继续有效。
- E5“dsh 有等价层”每条必须能指出 dsh 侧的对应物，否则降级为设计收割或补实现——本轮已按此标准清理。
- 蒸馏优先：移植“算法核心 + 注入缝”，不背运整个 crate；每个蒸馏件必须有测试与 NOTICE 溯源。
